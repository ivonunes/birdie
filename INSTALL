#!/bin/sh

# -----------------------------------------------------------------------------
# BUILDING
# -----------------------------------------------------------------------------

git clone https://github.com/birdieapp/birdie.git
cd birdie
mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX=/usr
make
sudo make install
