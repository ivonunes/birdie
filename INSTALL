#!/bin/sh

# -----------------------------------------------------------------------------
# BUILDING
# -----------------------------------------------------------------------------

git clone https://github.com/ivonunes/birdie.git
cd birdie
mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX=/usr
make
sudo make install
